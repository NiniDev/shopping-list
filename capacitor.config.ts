import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.ninidev.shoppinglist',
  appName: 'shopping list',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
